// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: query.sql

package db

import (
	"context"
	"database/sql"

	"github.com/ohhfishal/resume-wizard/resume"
)

const getApplicationHistory = `-- name: GetApplicationHistory :many
SELECT id, user_id, resume_id, status, company, position, description, created_at from application_history
WHERE user_id = ?
`

func (q *Queries) GetApplicationHistory(ctx context.Context, userID int64) ([]ApplicationHistory, error) {
	rows, err := q.db.QueryContext(ctx, getApplicationHistory, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ApplicationHistory{}
	for rows.Next() {
		var i ApplicationHistory
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.ResumeID,
			&i.Status,
			&i.Company,
			&i.Position,
			&i.Description,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getResume = `-- name: GetResume :one
SELECT id, user_id, name, resume, created_at, updated_at, last_used, deleted_at from resumes
WHERE user_id = ? AND id = ?
`

type GetResumeParams struct {
	UserID int64 `json:"user_id"`
	ID     int64 `json:"id"`
}

func (q *Queries) GetResume(ctx context.Context, arg GetResumeParams) (Resume, error) {
	row := q.db.QueryRowContext(ctx, getResume, arg.UserID, arg.ID)
	var i Resume
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Resume,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.LastUsed,
		&i.DeletedAt,
	)
	return i, err
}

const getResumes = `-- name: GetResumes :many
SELECT id, user_id, name, resume, created_at, updated_at, last_used, deleted_at from resumes
WHERE user_id = ?
ORDER BY created_at
`

func (q *Queries) GetResumes(ctx context.Context, userID int64) ([]Resume, error) {
	rows, err := q.db.QueryContext(ctx, getResumes, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Resume{}
	for rows.Next() {
		var i Resume
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Resume,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastUsed,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const insertApplicationActivity = `-- name: InsertApplicationActivity :one

INSERT INTO application_history (
    user_id,
    resume_id,
    company,
    position,
    description,
    status
) VALUES (
    ?, ?, ?, ?, ?, ?
) RETURNING id, user_id, resume_id, status, company, position, description, created_at
`

type InsertApplicationActivityParams struct {
	UserID      int64          `json:"user_id"`
	ResumeID    int64          `json:"resume_id"`
	Company     string         `json:"company"`
	Position    string         `json:"position"`
	Description sql.NullString `json:"description"`
	Status      string         `json:"status"`
}

// Last used??
func (q *Queries) InsertApplicationActivity(ctx context.Context, arg InsertApplicationActivityParams) (ApplicationHistory, error) {
	row := q.db.QueryRowContext(ctx, insertApplicationActivity,
		arg.UserID,
		arg.ResumeID,
		arg.Company,
		arg.Position,
		arg.Description,
		arg.Status,
	)
	var i ApplicationHistory
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.ResumeID,
		&i.Status,
		&i.Company,
		&i.Position,
		&i.Description,
		&i.CreatedAt,
	)
	return i, err
}

const insertResume = `-- name: InsertResume :one
INSERT INTO resumes (user_id, name, resume)
VALUES (?, ?, ?)
RETURNING id, user_id, name, resume, created_at, updated_at, last_used, deleted_at
`

type InsertResumeParams struct {
	UserID int64         `json:"user_id"`
	Name   string        `json:"name"`
	Resume resume.Resume `json:"resume"`
}

func (q *Queries) InsertResume(ctx context.Context, arg InsertResumeParams) (Resume, error) {
	row := q.db.QueryRowContext(ctx, insertResume, arg.UserID, arg.Name, arg.Resume)
	var i Resume
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Resume,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.LastUsed,
		&i.DeletedAt,
	)
	return i, err
}
